# Правила
## Правила работы (общие)
1. После каждого значимого действия обновляю этот файл (кратко)
2. Ты говоришь "восстанови" — я читаю и продолжаю
3. "добавь в правило - ..." — добавляю новый пункт в секцию ## Правила работы (общие)
4. "добавь локальное правило - ..." — добавляю правило в секцию ## Локальные правила работы
5. Все служебные скрипты и временные файлы сохраняй в папке ~/.koda_tmp
6. Для восстановления сессии нужно писать /восстанови (это правило для пользователя)
7. Если в папке проекта нет git-репозитория — предлагаю создать его
8. НЕ использовать зарезервированные слова (this, width, height, children, container и т.д.) для названий переменных, функций, методов, свойств
---

## Локальные правила работы (проект)
- Доступ к игре из локальной сети: http://192.168.1.91:8080
- После перезапуска WSL запустить `.koda_tmp/wsl_proxy.bat` для восстановления проброса портов
- Основная ветка разработки: папка `pixi/` (Pixi.js версия)
- Папка `my/` - старая webpack версия (не трогаем)
- После изменений в `pixi/` проверяем сборку: `npm run build:fast` (быстрая, без обфускации)
- Полная сборка с обфускацией: `npm run build` (для прода)
- При необходимости запустить dev-сервер прошу пользователя сделать это вручную (`npm run dev`), т.к. команда зависает
- Все цвета выносим в файл data/colors.js, только в HEX
- Сборка (`npm run build`) автоматически генерирует версию в формате v{package.json.version}-{git-hash}
- Логи: использовать `log()` из config.js (работает только при debug: true), `console.log()` только для временной отладки
- Все тексты и сообщения пишем через i18n (файлы `public/assets/lang/ru.json`, `en.json`)
- Разработку ведём на русском языке, en.json заполним перед релизом

### UINode - базовый класс для UI компонентов

**Зачем:** Единая система координат (pivot по центру), автоматические debug рамки, scale анимация.

**Структура:**
```javascript
import { UINode } from './ui_node.js'

export class MyComponent extends UINode {
  constructor(app, options = {}) {
    // width/height - для pivot по центру и debug рамки
    super({
      width: 300,
      height: 350,
      app: app,           // для scale анимации через ticker
      scaleSpeed: 0.15   // скорость lerp анимации scale
    })
    
    // Своя инициализация
    this.create()
    this.updateDebug() // Рисует debug рамку если debug: true
  }
  
  create() {
    // Добавляем children
    // Позиции от центра (благодаря pivot)
    this.addChild(someElement) // someElement.x = 0 - центр
  }
}
```

**Важно:**
1. Вызывать `create()` или всю логику рендера В КОНСТРУКТОРЕ (как Portal), НЕ отдельно через render()
2. Не использовать `render()` как отдельный метод — это путает (как было с EnemyDisplay)
3. setX/setY учитывают pivot: `x = visualX + pivotX`, поэтому визуальная позиция = центр элемента
4. При позиционировании через battle.js: `enemyDisplay.setX(screen.width/2)` — это ЦЕНТР, не левый край

**Пример использования в battle.js:**
```javascript
this.enemyDisplay = new EnemyDisplay(this.app, this.enemyData, this.assets)
this.enemyDisplay.setX(this.app.screen.width / 2) // Центр экрана
this.enemyDisplay.setY(280)
this.container.addChild(this.enemyDisplay)
```

**Компоненты на UINode:** Button, Circle, Portal, MapNode, EnemyDisplay

---

## Текущий контекст
Мы работаем над переносом карточной игры на Pixi.js.

## Актуальное состояние
- Перенос на Pixi.js завершён
- Бой и карта работают полностью
- Графика загружается из ассетов (pixi/public/assets/)
- Диалоги врагов при начале боя (Dialog класс)
- Система дебаффов работает (weaken_selected, block_buff)
- DiscardBuff применяет permanent баффы (не сбрасываются при выборе)
- Класс Player с localStorage (gold, crystals, wins, maps)
- 3 колоды в deck.js
- Portal с защитой от повторного destroy
- Git-репозиторий на GitHub
- Обфускация кода при сборке (javascript-obfuscator)
- DeckEditor - перерисовка контента без пересоздания модалки
- **Динамическая система наград:** золото за победу/ход/сверхурон, кристаллы только за босса
- **Оптимизированы экраны победы/поражения:** компактные отступы, корректное позиционирование
- **Убрано дублирование уведомлений** о наградах (показываются только в модалке)

---

## TODO (новый функционал) - по приоритету

### 0. Рубашки колод (DeckSleeve)
- [x] JSON: deck_sleeves.json (id, name, image, turns, discards, rarity, cost)
- [x] minCards = (turns + discards/2) × handSize (handSize = 8)
- [x] CollectionManager - расширить для хранения рубашек
- [x] DeckManager - добавить sleeveId каждой колоде
- [x] Battle - читать параметры рубашки при инициализации
- [x] UI: секция выбора рубашки в редакторе колоды

### 1. Игровая экономика
- [x] Золото — валюта для покупки карт и рубашек
- [x] Кристаллы — валюта для входа в портал (платные порталы с лучшей наградой)
- [x] Баланс 9/1 (90% золото, 10% кристаллы от награды)
- [ ] Ежедневные награды за вход
  - [ ] При входе на базу - диалог с Копещицей
  - [ ] Рандомная фраза (как на экране победы)
  - [ ] Фразы в lang файле
  - [ ] Сумма в config.js
  - [ ] Проверка: награда раз в 24ч (не чаще)
- [ ] Торговец (магазин на карте)
  - [ ] Покупка карт за золото
  - [ ] Покупка рубашек за золото
  - [ ] Случайное появление на карте (~25% шанс)

### 2. Магазин
- [ ] Магазин карт
- [ ] Магазин cash (золото/мана/души)

### 3. Интерфейс
- [x] Экран входа в игру с загрузкой (loading_screen.js, start_screen.js)
- [x] Редактор колоды (DeckEditor)
  - [x] Текстовое поле для названия колоды (Editable)
  - [x] Отображение всех карт из коллекции (haveInCollection, inDeck)
  - [x] Кнопки + / - для добавления/удаления карт
  - [x] Валидация в реальном времени (мин/макс размер, доступность)
  - [x] Кнопки "Сохранить" и "Сбросить"
  - [x] Переключение между колодами (табы/выпадающий список)
- [ ] Покупка новых карт
- [ ] Интерфейс настроек

### 4. Экраны победы/поражения (приоритет)
- [x] Убрать "Очки: NNN" из экранов победы и поражения
- [x] Перепозиционировать статистику после удаления Очков
- [x] Добавить эпические фразы при победе ("Слава герою!", "Победа за тобой!" и т.д.)
- [x] Исправить форматирование точек в статистике (ровные линии)

### 5. Дебаффы врагов (продолжение)
- [ ] Простые враги: дебафф - блокирует способность такой же карты в руке игрока
- [ ] Боссы: уникальные дебаффы
  - [ ] Отключить определённый бафф
  - [ ] Уменьшить силу удара
  - [ ] Уменьшить сборы
  - [ ] Уменьшить ходы
  - [ ] Сбросить определённые карты в колоде

### 5. Улучшения UI
- [ ] Drag&drop карт в руке
- [ ] Заменить alert/prompt на игровые UI (на основе Modal)
- [ ] Заменить emoji 💰/💎 на иконки (PNG 24x24 или 32x32)

### 6. Рефакторинг (в отложенном списке)
- [ ] Оптимизация `battle.js` (1260 строк)
  - Вынести логику боя в `BattleLogic`
  - Вынести обработку событий в `BattleEventHandler`
  - Риск: потеря связей между компонентами, регрессия
  - Приступить только после добавления юнит-тестов
- [ ] Оптимизация `deck_editor.js` (810 строк)
  - Вынести `DeckValidator` (валидация колоды)
  - Вынести `DeckEditorUI` (кнопки, табы, инпуты)
  - Оставить в `DeckEditor` только координацию
- [ ] Оптимизация `card.js` (767 строк)
  - Вынести `CardLogic` (баффы, дебаффы, вычисление значения)
  - Оставить в `Card` только рендеринг и анимации
- [ ] Оптимизация `game.js` (624 строки)
  - Вынести `ScreenManager` (управление переходами между экранами)
  - Вынести `ScreenState` (текущий экран, данные перехода)

## Реализованные фичи
- [x] Бой и карта работают полностью
- [x] BaseScreen (экран базы игрока)
- [x] Castle - класс замка с модальными окнами (Хранилище, Колода)
- [x] CardGridRenderer - универсальный рендер карт в сетку со скроллом
- [x] CollectionManager - управление коллекцией карт (загружается из collection.json)
- [x] DeckManager - управление колодами (загружается из decks.json)
- [x] EnemyDisplay, HandRenderer, DeckMenu, BattleUI (вынесены из battle.js)
- [x] MapNode, MapRenderer (вынесены из map.js)
- [x] Dialog класс для диалогов с героями
- [x] Диалоги врагов при начале боя (enemies.json + dialog)
- [x] Система дебаффов (weaken_selected, block_buff)
- [x] permanent баффы (DiscardBuff)
- [x] Класс Player с localStorage (gold, crystals, wins, maps)
- [x] 3 колоды в deck.js
- [x] Portal с защитой от повторного destroy